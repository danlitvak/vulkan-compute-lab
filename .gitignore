build/
out/
# F12 captures. Personal output rather than project content, and a 1280x720 PNG
# from src/png.cpp is ~3.7 MB; curated images live in docs/ instead. The
# directory itself is tracked via .gitkeep so it exists in a fresh clone.
screenshots/*
!screenshots/.gitkeep
.vs/
.cache/
*.spv
CMakeUserPresets.json
